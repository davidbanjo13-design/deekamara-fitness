import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  weight: ["300", "400", "500", "600", "700", "800"],
  subsets: ["latin"],
  variable: "--font-poppins",
});

export const metadata: Metadata = {
  title: "DeeKamara Fitness Coaching | Transform Your Life",
  description: "Professional fitness coaching with DeeKamara. Online and in-person coaching to help you achieve your fitness goals. Start your transformation journey today.",
  keywords: "fitness coaching, personal trainer, online coaching, weight loss, strength training, DeeKamara",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${poppins.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
